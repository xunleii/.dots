@AGENTS.md

# Commit messages

Use the symbol-based convention in [docs/COMMITS.md](docs/COMMITS.md):
`type[scope]: Subject` (e.g. `+[fish]: Add nono-profile picker`). Not
gitmoji shortcodes. Pick the scope from the list there; if none fits, ask
before inventing a new one.
