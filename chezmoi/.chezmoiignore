# --- Chezmoi configuration ---
.chezmoi.toml.tmpl

# --- Repository metadata ---
LICENSE
README.md

# --- Scripts
scripts/**

# --- Local-only, never synced (per-machine personality/tone)
.claude/PERSONNALITY.md

# --- Workspace directories (managed by git only)
.workspaces/**/.gitkeep